# 套件评审记录 2026-09-22 initial B

本文件归档 reviewer B 已完成的第一次独立初审，不是对修复工作区的重新评审。原始结论为 **C：BLOCKED**。后续修复不反向改变本记录；只能通过新的固定基线与定向复核继承证据。

## 基线、规则和执行边界

- 标准：TAD-REVIEW-EXIT / 1.0；SHA-256：`5937af637dbaf681f0c3046a1a3643de2dd0a3be6ee55ae9b4421aacff9e0393`。
- 仓库：`/Users/tom/Desktop/skills/tom-autodev-suite`；分支：`phase1-workflowspec`。
- target_commit：`273d2507bcfb600d1a1ab9e5e6d8f61b72d41565`；base_commit：全量初审，不适用。
- 审查快照：`/tmp/tad-review-273d250-elgq4r02`，无 `.git` 的归档。
- 目标层级/范围：C 工程层 WF-01…WF-10；skill/schema/controller 合同、worker 与维护直连入口，以及正常、错误、恢复、并发和发布路径。
- 支持环境的实际取证：macOS 26.6.2 arm64、Python 3.9.6，临时 Git、SQLite 与模拟平台。没有执行真实平台调用或业务项目。
- workflow：`workflow-spec-v2`；规范 hash：`71f51bcabcd042cf3870e78e57568b0940e0288734c4be4b14a6398aaffb71c6`。
- 16 个 schema 和 12 个 SKILL.md 的内容清单 hash：`408b8bf428616a7f0bb75dad8ee8cf7a4a16c1fbdae03ab9c145d9b48dfa1985`。算法：按相对路径排序，拼接每项 `path + NUL + SHA256(file).digest()`，再求 SHA-256。
- profile 身份：没有真实业务 profile；使用目标 commit 中的测试 fixture。未生成 M 层 producer prompt/原始模型响应。
- 评审者/模型/日期：Codex 独立上下文 B；具体模型版本未获得可验证身份；2026-09-22。
- 负责人：评审发起人或指定维护者，尚无风险接受决定。
- 预算：一次有界初审，零修复轮次；没有读取其他 reviewer 结论，也没有启动子 agent。
- 本记录没有继承其他评审报告的通过结论。历史整合记录仅作线索，所有确认问题重新核对当前代码。

### 快照差异说明

逐一比对目标 commit 的 311 个 Git blob，310 个一致。唯一差异为 `tom-autodev/infoflow-gateway/package-lock.json` 根依赖：

```diff
-        "@baidu/infoflow-sdk-nodejs": "^2026.8.28"
+        "@baidu/infoflow-sdk-nodejs": "2026.8.28"
```

文件修改时间在本轮运行期间。B 未将变化归因到具体并发调用。所有 findings 引用的源码均与目标 commit 一致；全量测试环境不能在不说明此差异的情况下称为字节完全未变。第一次 unittest 未设置 `PYTHONDONTWRITEBYTECODE`，已向汇总者报告潜在缓存产物；后续 Python 取证均禁止生成 bytecode。

## 原始验证结果

在指定快照执行：

```text
$ python3 -m unittest discover -s tom-autodev/scripts/tests -p 'test_*.py' -q
----------------------------------------------------------------------
Ran 911 tests in 122.706s

OK

$ PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tom-review/tests -p 'test_*.py' -v
test_disables_external_diff_and_textconv ... ok
test_does_not_execute_clean_filter_when_worktree_differs ... ok
test_empty_diff_and_mode_only_changes_remain_visible ... ok
test_rejects_moving_refs_and_missing_objects ... ok
test_special_paths_binary_deletions_and_renames_are_not_lost ... ok
test_uses_pinned_commits_and_preserves_dirty_workspace ... ok
----------------------------------------------------------------------
Ran 6 tests in 0.898s

OK

$ PYTHONDONTWRITEBYTECODE=1 bash tom-diagnose/tests/test_autodebug.sh
PASS: tom-autodebug regression tests
```

另在原仓只读执行 `git diff --check 273d2507bcfb600d1a1ab9e5e6d8f61b72d41565^ 273d2507bcfb600d1a1ab9e5e6d8f61b72d41565`，退出 0。这是目标提交差分检查，不冒充归档快照的工作区检查。

## 验收矩阵：初审时点

| ID | 实际查证范围、证据与结果 | 结论及未覆盖项 |
|---|---|---|
| WF-01 | producer/schema/phase-protocol repair 回归通过；核对 task、revision、前驱、traceability 校验和旧非法草案 CAS 重开；Review 未完成/澄清有拒绝或停止证据。 | 失败：B-01 表明维护 advance 绕过实质产物证明。正常 producer 校验不能推广到全部入口。 |
| WF-02 | 错 hash、成员、拒绝、超时、重复回应和重试上限回归通过；以真实 ApprovalLedger 和 PLAN fixture 定向复现重发。 | 失败：B-02。现有 retry 回归只覆盖 iCode/iPipe 底层消费者，没有覆盖 worker/EvidenceGate 完整链。 |
| WF-03 | workspace recovery、task5 safety、iCode preflight 通过；核对固定基线、owner、错误仓/版本、脏源仓保留、reserved/removing 恢复及业务/独立测试 revision 绑定。 | 所查路径通过；没有遍历真实项目 profile 的仓库拓扑，不能作为真实平台资格证据。 |
| WF-04 | phase publication、artifact/transition 断点回放、保存草案获批后消费、merged SPEC/TASKS 恢复、iCode 未知提交只查询、iPipe intent 查询有回归。 | 部分失败：B-02 阻断合法重发审批后的恢复；真实平台最终一致性延迟未验证。 |
| WF-05 | 核对 worker.advance 实际租约装配、locks=None 边界、SQLite CAS、workspace fcntl 操作锁；双线程实际驱动同 run，第二 worker 返回 WORKER_LEASE_HELD，最终仅一个 PLAN 产物，状态 IMPLEMENT，租约释放；死 PID+TTL 接管回归通过。 | 初审缺口：没有执行两个不同 run 同时竞争同一源仓资源的完整 worker 场景；真实宿主锁装配未验证。此缺口后续取证另记，不改本初审时点。 |
| WF-06 | pipeline plan/ipipe runtime 回归通过；核对冻结计划、依赖闭包、共享测试变化、独立模块复用、同 change ID 新 revision、旧 build 失效与 final archive 后恢复。 | 正常调度/摄取有证据；B-01 可从维护推进路径跳过缺失模块，整体不能判通过。 |
| WF-07 | 正常 worker 与直接 ingest 的计划、全部模块、runtime verification receipt 校验通过；另执行平台未发布和缺模块两种 advance 触发。 | 失败：B-01。 |
| WF-08 | execution guard、workflow spec、profile repin 回归通过；核对 workflow 漂移守卫、纯回执返回、明确 repin 与 legacy plan 恢复前提。 | 所测路径通过；没有穷尽全部维护入口在 profile 漂移下的实际副作用，不能从 workflow-only 矩阵推出 profile 全入口等价。 |
| WF-09 | 修复策略已接入 completion_target，持久化历史、上限、签名归一与跨 run 计数回归通过；重新执行无 diff/无 pipeline ID 的诊断校验。 | 失败：B-03/B-04；没有复用“预算未接线”的历史描述作为新缺陷。 |
| WF-10 | 回执身份、产物完整性、缓存 prompt/model 匹配、secret-key 拒绝、summary 脱敏回归通过；核对模拟/真实证据边界。 | 失败：B-01 可产生没有发布回执/产物的成功终态；M/P 均未验证。 |

## Findings：固定 273d250 基线

下列代码行号全部针对上述 target_commit/快照，不表示修复工作区当前行号。

### B-01 / P1：维护 advance 在没有发布证据时写 RELEASE_SUCCESS

- 违反合同：WF-01、WF-07、WF-10，尤其 WF-07 要求直接推进与 worker 同约束。
- 触发：以 `PipelinePlanTests` 创建 A/B 两必需模块的冻结计划，两个 build 都 SUCCESS，但模拟平台 `releases=[]`。当前 RELEASE action 的准确 G9 已批准。向 `Orchestrator.advance(run,"RELEASE_SUCCESS",ctx)` 提供该相同批准、正确环境指纹、相同 revision 对及 `artifacts=["release-evidence"]`。
- 预期：缺少真实发布与 RELEASE 产物/runtime verification receipt，应拒绝。
- 实际：正常 worker 返回 RELEASE_WAITING；直接 advance 返回 OK/RELEASE_SUCCESS，持久状态为 RELEASE_SUCCESS，RELEASE 产物列表为空。
- 第二触发：仅 A 构建通过、B 尚无证据，直接 advance 仍可连续 IPIPE→RELEASE→RELEASE_SUCCESS，最终仍无 RELEASE 产物。
- 当前代码证据：`orchestrator.py:1115-1146` 仅调用通用 EvidenceGate 后提交状态；`evidence_policy.py:89-96` 将调用方字符串集合视为可用产物；CLI `orchestrator.py:3477-3483` 明确保留调用方名称断言。
- 反证：环境钉定存在，但不检查冻结计划、逐模块 build 或发布核验回执。`phase_protocol.py:1729` 的 release binding 校验不在 advance 调用链上。`test_pipeline_plan.py:277` 所谓 direct release 测试实际只调用 ingest，没有覆盖 advance。
- 影响：错误认定交付成功并进入终态；本次未声称执行了平台发布。
- 处置：确认、初审时未修复，阻断 C。

原始关键输出：

```json
{
  "state_before": "RELEASE",
  "required_modules": ["A", "B"],
  "platform_releases": [],
  "worker_result": {
    "ok": true,
    "reason_code": "PARKED",
    "parked": "RELEASE_WAITING",
    "controller": "release",
    "module": "A",
    "build_id": "build-A"
  },
  "direct_advance": {"reason_code": "OK", "state": "RELEASE_SUCCESS"},
  "final_state": "RELEASE_SUCCESS",
  "release_artifacts": []
}
```

另一次缺模块触发输出：

```json
{"state_before":"IPIPE","required":["A","B"],"built_modules":["A"],"advance_to_release":{"reason_code":"OK","state":"RELEASE"}}
{"advance_to_success":{"reason_code":"OK","state":"RELEASE_SUCCESS"},"release_artifacts":[]}
```

### B-02 / P1：合法重发批准仍被 worker 和 EvidenceGate 拒绝

- 违反合同：WF-02 合法重发可消费、WF-04 审批恢复。
- 触发：PLAN 草案已保存；原 G4 到期并写为 TIMEOUT。同 run、同准确 hash 的 G4#retry-1 双渠道投递后合法 APPROVE。
- 预期：从已保存草案完成 PLAN。
- 实际：worker.submit_draft 返回 APPROVAL_REQUIRED；使用重发 approval ID 直接 complete_phase 相同内容也返回 APPROVAL_REQUIRED，状态保持 PLAN。
- 当前代码证据：`worker_driver.py:73,157` 和 `evidence_policy.py:57` 使用原始 `action == gate`。
- 反证：PhaseProtocol._approval_error 在 1135 用 gate_of，但 1142 又进入 EvidenceGate，因此归一不完整。iCode/iPipe approved_record 的修复和现有 retry 测试确实通过，不能证明 worker/phase consumers 通过。原批准终态、合法草案 FULFILLED，重投不同内容不能正常替换；没有验证到受支持的可行绕行。
- 处置：确认、初审时未修复，阻断 C。

原始关键输出：

```json
{
  "original": "TIMEOUT",
  "retry": "APPROVE",
  "worker_result": {"ok": false, "reason_code": "APPROVAL_REQUIRED", "gate": "G4"},
  "direct_complete": {"ok": false, "reason_code": "APPROVAL_REQUIRED", "phase_complete": false},
  "state": "PLAN"
}
```

### B-03 / P1：无补丁的已确认 REPAIR 诊断不可表达

- 违反合同：WF-09 指定场景；`tom-diagnose/SKILL.md:24` 禁止 DIAGNOSE 写补丁，`tom-diagnose/references/phase-contract.md:12` 仅允许引用真实已有 diff。
- 触发：route=REPAIR、evidence_state=SUFFICIENT、repair_scope=CODE_ONLY，根因和计划完整，repair_diff_hash=null。
- 预期：接受真实修复提案，经 G6 进入 PLAN/IMPLEMENT。
- 实际：纯 JSON Schema 校验通过；PhaseProtocol 完整语义校验报 SCHEMA_INVALID，repair_diff_hash missing。
- 当前代码证据：`schema_validator.py:462-465` 要求 REPAIR 已有非空 diff hash；producer 使用相同语义校验，重试不能解除该矛盾。
- 反证：其他 route 的 null 支持不解决 CODE_ONLY 修复。改 route、造 hash、先写代码均违背合同。已有路由/预算用例带非空 hash，不覆盖此场景。
- 处置：确认、初审时未修复，阻断 C。旧文档披露不等于风险接受。

原始关键输出：

```json
{
  "route": "REPAIR",
  "schema_only": [],
  "phase_validator": {
    "ok": false, "reason_code": "SCHEMA_INVALID", "phase_complete": false,
    "content_invalid": true, "schema": "diagnosis",
    "schema_errors": [{"path":"repair_diff_hash","kind":"missing"}]
  }
}
```

### B-04 / P1：源码 Review 失败缺少可表达的诊断身份合同

- 违反合同：WF-09 源码 Review 失败身份；Diagnose phase contract 第 7/18 行禁止编造 pipeline IDs，要求父控制器明确映射。
- 触发：iCode/iPipe 前源码 Review 出现确认阻断项，`phase_protocol.py:1209-1210` 正常路由 DIAGNOSE，此时没有 build/stage/job。
- 预期：通过真实 Review artifact/finding 身份表达失败，或由父控制器提供明确非流水线映射。
- 实际：`diagnosis.schema.json:1` 无条件要求三个非空字符串；设 null 返回 build_id/job_id/stage_id invalid，省略则 missing。父级 action 仅提供前驱/task/revision，没有该映射。
- 反证：DIAGNOSIS_INCOMPLETE 也不豁免必填身份。已有 incomplete 诊断测试使用完整 fixture，保留 pipeline IDs，不能证明源码 Review 路径。合同中“若父级提供映射则使用”不构成当前实际映射。
- 处置：确认、初审时未修复，阻断 C；这是承诺场景的合同缺口，不是新增能力请求。

原始校验输出：

```text
review_failure_no_pipeline_ids= [('build_id', 'invalid'), ('job_id', 'invalid'), ('stage_id', 'invalid')]
```

## 剩余风险与处置台账

| ID | 范围与影响 | 绕行/理由 | 负责人及接受证据 | 到期/重开条件 |
|---|---|---|---|---|
| B-01…B-04 | 上述 P1 核心合同破坏 | 没有验证到可接受绕行；不得凭披露放行 | 发起人/指定维护者；无接受决定 | 修复后固定 commit，定向复核真实触发与正常调用方 |
| GAP-WF05 | 初审未完成跨 run 共享源仓实際 worker 竞争 | 后续授权定向补证，单列报告 | 发起人 | 补证完成后记录证据继承，不改原始 finding |
| GAP-WF08 | 未穷尽 profile 漂移入口 | 后续只补影响结论所需路径 | 发起人 | 精确入口/调用方证据补齐 |
| SNAPSHOT-LOCK | package-lock 根 semver 范围有一字符变化 | 所有 finding 源码已与 commit 对齐 | 汇总者核对 | 明确差异原因/最终依赖基线 |
| M/P | 真实模型和平台资格未验证 | 不从 C 模拟测试推断 | 发起人/目标平台负责人 | 按独立 M/P 标准取证 |

## 初审结论

- C：**BLOCKED**，4 项确认 P1；并有矩阵和快照差异所述缺口。
- M：**未验证**；没有指定模型 8×3 固定行为资格测试。
- P：**未验证**；没有真实 iCode/iPipe/发布/业务环境证据。
- 无风格重构和额外能力建议。初审到此停止；后续合并同根因并只对确认修复、受影响调用方和必要缺口做定向复核。
