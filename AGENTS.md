# 仓库维护与评审

本文件约束 `tom-autodev-suite` 自身的维护；业务项目 run 仍遵守各阶段和项目合同。

评审、复核或判断本套件是否完成时，先读取唯一标准：
[docs/WORKFLOW_EXIT_CRITERIA.md](docs/WORKFLOW_EXIT_CRITERIA.md)。
按其中的基线、证据、严重级别、报告模板和停止/重开规则工作。不要用模型偏好新增本轮验收目标。

修复评审问题时，核对 finding 的合同和当前证据，再做有界修复与对应验证。完整工程验收和局部改动检查的区别由该标准定义。

将本文件路径按仓库根目录解析；若通过已安装 skill 的链接进入，先定位实际套件目录。规则或代码不可读时明确报告缺口，不宣称已按标准通过。
